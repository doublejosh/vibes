> This project existings solely to be a presentation with 20 slides. Let's first setup a few basic manifest files to describe what this is to keep as a constant reference. We should also keep the content separate from the presentation. Create an index.html file that we'll work on to see this in a browser.
 
> Create a file to track all the prompts used to create this project. Make the first two entries be the first and this one.

> The PROMPT file needs to be more simple. Just list the prompts with a timestamp, the results of each should be 3 bullet points.

> Make a way to stash style per slide so the look can evolve as the slides advance.

> The entire screen is the slide, remove any presentation wrapper. The controls can live in the bottom left corner. Add keyboard controls with left and right arrows to control the slide advance too.

> Go ahead and create the styles-per slide for all 20. Start very clean and elegant and simple, get more colorful and interesting each time, but 10 slides in it should start to get wacky and weird, and eventually totally over the top in the last few slides.

> Display the name of the animation keyframe used in the customCSS in the bottom right of the slide if one is used.

> Make the slide full screen without a white border. Remove the secondary counter inside the slide, there's already a navigation. Move the animation name label to a floating position like the navigation.

> The last few slides went too far. We can't read the text content at all. The last 2-3 also cause the browser to bog down due to hardware issues with the CSS.